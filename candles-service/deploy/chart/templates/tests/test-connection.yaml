apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "candles-chart.fullname" . }}-test-connection"
  labels:
    {{- include "candles-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "candles-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
